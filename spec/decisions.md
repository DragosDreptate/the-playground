# Décisions clés — The Playground

> **Registre canonique des décisions du projet.** Source unique de vérité.
> Journal chronologique des décisions structurantes (architecturales, produit, stratégiques, techniques) prises au fil du projet. Les décisions intermédiaires supersédées ont été retirées.
>
> **Deux niveaux** : une ligne ici pour toute décision structurante ; un ADR détaillé dans [`spec/decisions/`](decisions/) (Contexte → Décision → Alternatives écartées → Conséquences) pour les décisions vraiment lourdes. Voir [`spec/decisions/README.md`](decisions/README.md).
>
> Règles de vie du projet (terminologie, principes, stack) : `CLAUDE.md`.

---

| Date | Décision |
| --- | --- |
| 2026-02-19 | Usecases = fonctions (pas de classes). ActionResult pattern pour server actions. Slug généré dans le usecase (règle métier). |
| 2026-02-19 | Stack : TypeScript full-stack (Next.js 16, Prisma, PostgreSQL, Auth.js, Stripe Connect, Tailwind + shadcn/ui, Resend, Anthropic SDK, Vercel + Neon). |
| 2026-02-19 | Architecture hexagonale stricte : `domain/` (models, ports, usecases) + `infrastructure/` (repositories, services) + `app/` (routes Next.js). |
| 2026-02-19 | Tests BDD lightweight (Given/When/Then natif Vitest) + Specification by Example (`test.each`), pas de Gherkin/Cucumber. |
| 2026-02-19 | 100% gratuit, 0% commission plateforme, seuls frais Stripe. |
| 2026-02-19 | Auth : Magic link + OAuth (Google, GitHub) via Auth.js v5. |
| 2026-02-19 | UI bilingue FR/EN dès V1, architecture i18n native (next-intl). |
| 2026-02-19 | Track retiré du MVP V1 → Phase 2. Check-in retiré du MVP → Phase 2. |
| 2026-02-20 | Modèle User unique — Host = Player + droits de gestion (rôle via `CircleMembership`). |
| 2026-02-20 | Positionnement : community-centric (modèle Meetup) + UX premium (Luma) + 100% gratuit. Communauté = entité centrale, événement = porte d'entrée virale. |
| 2026-02-20 | L'Organisateur est automatiquement inscrit (REGISTERED) à l'événement qu'il crée. |
| 2026-02-20 | Email non éditable dans le profil (clé unique Auth.js). Pas de merge/liaison de comptes dans le MVP. |
| 2026-02-20 | Neon branching dev/prod (`pnpm db:dev:reset`). Prix en centimes (int, convention Stripe). |
| 2026-02-21 | Fil de commentaires plat (pas de réponses imbriquées). Max 2000 chars. Tout authentifié peut commenter. |
| 2026-02-21 | Convention données test : domaine `@test.playground` (dev + prod). Convention démo : `@demo.playground`. |
| 2026-02-21 | Badges unifiés : `default` = engagement positif, `outline` = tout le reste. Couleur unique : `destructive` = `primary` (même rose). |
| 2026-02-21 | Emails envoyés depuis server actions (pas usecases). Fire-and-forget (email échoue → inscription réussit). |
| 2026-02-21 | Sécurité defense-in-depth : chaque usecase admin accepte `callerRole: UserRole` + lève `AdminUnauthorizedError`. |
| 2026-02-22 | **Terminologie FR finale** : Communauté (féminin), événement (masculin), Organisateur, Participant, Mon espace, Explorer, S'inscrire. Code inchangé (Circle, Moment, Host, Player). |
| 2026-02-23 | **Terminologie EN finale** : Community, Event, Member, Dashboard, Explore. Code inchangé. |
| 2026-02-28 | Dashboard Mode Switcher : enum `DashboardMode` (`PARTICIPANT`/`ORGANIZER`) persisté en DB + session Auth.js. |
| 2026-03-03 | Broadcast cooldown 24h (remplace le verrou permanent "envoi unique"). `broadcastSentAt` écrasé à chaque envoi. |
| 2026-03-10 | Profils publics utilisateurs `/u/[publicId]` (accessibles aux connectés). Slug auto `prénom-nom-XXXX` (`publicId` nullable + backfill), participants cliquables. Levier de viralité / social proof. `spec/features/virality-public-profiles.md`. |
| 2026-03-13 | **Score de pertinence Explorer** (`explorerScore` 0-100, invisible) recalculé chaque nuit par cron, à partir de signaux objectifs (membres, inscriptions, récence), `overrideScore` admin + `excludedFromExplorer`. Écart délibéré et cadré au principe « pas de ranking global » (scope Explorer, déterministe, non personnalisé). **[→ ADR-0003](decisions/0003-explorer-score-vs-no-global-ranking.md)** |
| 2026-03-14 | Statut DRAFT : tout événement créé est en Brouillon. DRAFT → PUBLISHED via `publishMoment` (sens unique). Notifications déplacées à la publication. |
| 2026-03-30 | Explorer (découverte publique) : `CircleCategory` (enum 8 valeurs) + `city` (texte libre), filtre catégorie + tri Recommandé/Popularité/Date, section « À la une », pages Communauté/Événement publiques sans auth. `spec/features/explorer-la-carte.md`. |
| 2026-03-30 | **Paiements — Stripe Connect** : comptes **Express** (Stripe gère le KYC) + **destination charges** (le paiement transite par la plateforme avant reversement → autorise une commission future sans refactor). Standard/Direct écartés. Règles métier durables : prix verrouillé après la 1ʳᵉ inscription, mini 50 c, pas de liste d'attente sur payant, auth obligatoire avant paiement, frais Stripe absorbés par le Participant (affichage TTC). `spec/features/stripe-connect.md` (spec dédiée = doc profonde). |
| 2026-03-30 | **Validation des inscriptions par l'Organisateur** : `requiresApproval` indépendant sur Circle ET Moment (aucun héritage), statuts `PENDING_APPROVAL`/`REJECTED` (Registration) + `PENDING`/`ACTIVE` (membership), re-inscription après rejet réactive la ligne (pas de doublon). `spec/features/approval-registration.md`. |
| 2026-03-30 | Radar d'événements concurrents à la création (Luma API + scraping Eventbrite/Meetup), 10 résultats/plateforme, rate-limit 25 analyses/j/user. Aide l'Organisateur à éviter les collisions de dates. `spec/features/local-events-watcher.md`. |
| 2026-03-30 | Pièces jointes événement : max 3, types PDF/JPG/PNG/WebP, suppression en cascade. `spec/features/moment-attachments.md`. |
| 2026-04-03 | Blog statique Markdown (remark/rehype) pour le SEO : articles versionnés en `src/content/blog/{locale}/*.md`, JSON-LD `BlogPosting`, OG image dynamique. `spec/features/blog-seo.md` + `spec/infra/seo-strategy.md`. |
| 2026-04-08 | **Réseaux de communautés** : modèle `CircleNetwork` (N:N), page publique `/networks/[slug]`, badge « Membre de [Réseau] » sur la page Communauté, gestion admin (composition + ordre), pas de workflow d'invitation. Code = `CircleNetwork`, UI = « Réseau ». `spec/features/circle-network.md`. |
| 2026-04-08 | Suppression du token d'invitation Circle : le lien public `/circles/[slug]` devient l'unique porte d'entrée (route `join/[token]` supprimée). Check HOST désormais explicite dans `inviteToCircleByEmailAction`. `spec/features/remove-invite-token.md`. |
| 2026-04-12 | Email d'onboarding = **1 seul email** (lettre du fondateur), expéditeur dédié `dragos@the-playground.fr`, idempotence `welcomeEmailSentAt`, exclusion `@test`/`@demo`. `spec/features/email-onboarding.md`. |
| 2026-04-16 | Email transactionnel : on reste sur **Resend** (DX React Email + Batch API, free tier suffisant ; alternatives EU jugées immatures / nuisibles à la DX). `spec/infra/email-service-alternatives.md`. |
| 2026-04-17 | **Rôle `CO_HOST` (co-organisateurs)** : délègue la gestion (créer/éditer événements, approuver inscriptions, message aux participants) sans céder le contrôle (suppression Circle, promotion, Stripe Connect réservés au HOST). Tous les checks organisateur passent par `isActiveOrganizer()`. `spec/features/co-organisateurs.md`. |
| ≈ 2026-04 | Admin plateforme : `UserRole` (USER/ADMIN), port `AdminRepository` séparé, pages `/admin/*` gardées par `role === "ADMIN"` (complète la couche defense-in-depth du 2026-02-21). `spec/features/admin-plateforme.md`. |
| ≈ 2026-04 | Photos de commentaire : max 3, JPEG/PNG/WebP, redimensionnement client conditionnel (seuil 3 Mo), validation MIME par magic bytes côté serveur. `spec/features/comment-photos.md`. |
| 2026-04-18 | Refonte du backlog : fichier unique `spec/BACKLOG.md` au format minimaliste (5 champs fixes), items ajoutés uniquement sur demande explicite. Phase 2 + Stripe déplacés dans `spec/ideas.md`, décisions clés dans `spec/decisions.md`. |
| 2026-04-19 | `spec/BACKLOG.md` sorti du tracking git. Source unique dans le repo principal, symlink dans les worktrees créé automatiquement par `scripts/worktree-new.sh`. Modifications en local sans commit / PR. |
| 2026-04-22 | **Distinction « Rejoindre » vs « S'inscrire » (FR) verrouillée** comme choix volontaire non unifiable. Rejoindre = action sur un Circle (adhésion durable à une communauté). S'inscrire = action sur un Moment (RSVP ponctuel). En EN les deux contextes utilisent « Join ». Ne pas chercher à unifier en FR. |
| 2026-05-06 | Migration du backlog vers GitHub Issues + Project v2 « Backlog » (https://github.com/users/DragosDreptate/projects/1). Labels `type:*` (feature, evol, fix, chore, idea), fields Project `Status` (Backlog, Up Next, In Progress, In Review, Done) et `Priority` (high, medium, low). 22 items migrés en issues #423 → #444. `spec/BACKLOG.md` supprimé, symlink retiré du script worktree. |
| 2026-05-07 | `allowDangerousEmailAccountLinking: true` activé sur Google et GitHub. Évite l'erreur `OAuthAccountNotLinked` quand un User a un compte existant (créé via magic link ou autre provider) et tente de se connecter avec un OAuth pour la première fois sur le même email. Risque mitigé : Google vérifie l'email via OIDC (`email_verified`), GitHub exige une vérification avant qu'une adresse soit marquée comme primary. La page `/auth/sign-in` lit désormais `searchParams.error` et affiche un bandeau pour les `SignInError` qui transitent encore par cette page (`OAuthCallbackError`, `OAuthSignInError`, `AccessDenied`, fallback `Default`). Issue #429. **[→ ADR-0001](decisions/0001-oauth-account-linking.md)** |
| 2026-05-08 | Bouton de partage **mobile-only** (Web Share API, `lg:hidden`, masqué si `navigator.share` indispo). URL absolue (`NEXT_PUBLIC_APP_URL`, pas `window.location`), tracking PostHog seulement sur partage réussi (pas sur `AbortError`), masqué sur événements DRAFT/CANCELLED. `spec/features/share-button-participant.md`. |
| 2026-05-13 | Issues GitHub sans formulaire libre : `blank_issues_enabled: false`, toute issue passe par un des 5 templates (fix/feature/evolution/idea/chore) ou par le canal sécurité. Garde-fou pour un Project public. `.github/ISSUE_TEMPLATE/`. |
| 2026-05-21 | Widget embed événement (`/embed/m/[slug]?locale&theme`) : layout dépouillé, `revalidate=300s`, CSP `frame-ancestors *`, **pas** de tracking PostHog (RGPD en iframe tierce), DRAFT → 404. `spec/features/embed-event-widget.md`. |
| 2026-05-22 | Blocage des bots à deux niveaux : crawlers SEO agressifs bloqués site-wide (403 + robots `Disallow /`) ; bots d'entraînement IA bloqués **uniquement sur l'UGC** (`/m/`, `/circles/`, embed) — ce contenu appartient aux Organisateurs, pas à la plateforme. PR #496, `src/lib/blocked-bots.ts`. |
| 2026-05-27 | `noindex` sur toutes les routes du flow d'auth et les routes techniques ; seules les pages produit pertinentes sont indexées. PR #498. |
| 2026-05-28 | **Magic link réutilisable (15 min)** — pivot du token à usage unique : le token reste valide/réutilisable 15 min, `/auth/confirm` supprimé, locale lue du `callbackUrl`, fenêtre `isNewUser` élargie pour absorber le prefetch des scanners email. Motivé par un drop-off prod (comptes corporate). Conséquence durable : une row `verification_tokens` ≠ « lien non cliqué ». PR #501. **[→ ADR-0002](decisions/0002-reusable-magic-link.md)** |
| 2026-06-01 | Driver DB : **WebSocket Pool Neon** (`@prisma/adapter-neon`) verrouillé, **TCP exclu définitivement** (~120 ms vs ~15 ms/query). `spec/infra/neon-websocket-timeouts.md`. |
| 2026-06-03 | Quick win Neon stale-connection : timeouts réduits (`idleTimeoutMillis` 30s→8s, `connectionTimeoutMillis` 15s→5s) pour purger les WebSockets périmées. + Mode dégradé `degradedQuery()` (`noStore()` + log Sentry) : une requête décorative qui plante ne casse pas le CTA. `spec/infra/neon-websocket-timeouts.md`. |
| 2026-06-03 | Surveillance du quota Resend par cron dédié `/api/cron/check-resend-quota` (compte les emails du jour, alerte Slack aux paliers 70/90). Remplace le moniteur in-flight qui ne se déclenchait jamais. PR #511. |
| 2026-06-04 | Dispatch des notifications (emails + Slack) via **`after()`** (Vercel Fluid Compute), plus de fire-and-forget nu (largué au gel de l'instance → pertes intermittentes). Affine la décision fire-and-forget du 2026-02-21. PR #516/#519. |
| 2026-06-04 | Canal Slack dédié `#sentry` pour les alertes Sentry (`SLACK_SENTRY_WEBHOOK_URL`, fallback `#admin` si absent). PR #518. |
| 2026-06-10 | Observabilité auth côté serveur : capture Sentry server-side de chaque sign-in réussi + de la page `/auth/error`, car les réseaux corporate bloquent sentry.io côté client. Try/catch intégral (n'empêche jamais le sign-in). PR #526. |
| 2026-06-10 | « **Message aux participants** » remplace « Inviter ma communauté » sur l'événement (communication ciblée, pas acquisition) : usecase `SendMomentHostMessage`, segments REGISTERED/WAITLISTED/ALL, autorisation Host/Co-Host, bulk `resend.batch.send` (From « {Organisateur} via The Playground » + Reply-To Organisateur), éditeur Tiptap + sanitization serveur. Renommage `broadcastSentAt` → `lastHostMessageSentAt`. PR #527, `spec/features/moment-host-message.md`. |
| 2026-06-11 | Aucune donnée perso réelle en dur dans le repo (public) ; scripts nominatifs dans `scripts/local/` (gitignored, avec README). PR #531. |
| 2026-06-14 | **Anti-bot Vercel BotID sur le sign-in** (les 4 flux : Google/GitHub/LinkedIn/email), piloté par `BOTID_MODE` (enforce/observe/off). Wrapper **fail-open** (panne BotID → jamais bloquer un humain), event PostHog `bot_detected` via `after()`. Coupe à la racine le vecteur « contacter les organisateurs ». PR #538-540. **[→ ADR-0004](decisions/0004-botid-sign-in-fail-open.md)** |
| 2026-06-14 | Blocage des **domaines email jetables** au sign-in magic link (`isDisposableEmailDomain`, package `disposable-email-domains` + blocklist custom, sous-domaines/suffixes parents), classé `AccessDenied`. Suite à l'incident phishing du 14/06 (compte vérifié créé via email jetable). Advisory `GHSA-98r8-6pfp-28mh`. PR #534/#535. |
| 2026-06-14 | Blocage des comptes abusifs au sign-in via blocklist par **`providerAccountId` OAuth** (fiable, non falsifiable, vs le nom de profil) + garde anti-usurpation des noms de profil (support/admin/playground + variantes leet). Réponse à une attaque active (faux « PLAYGROUND SUPP0RT » préparant du phishing). PR #534, `src/infrastructure/auth/sign-in-blocklist.ts`. |
| ≈ 2026-06 | Adhésion Communauté via inscription à un événement = **une seule notif Host** (la notif événement, pas l'email « nouveau membre ») — choix délibéré, anti-avalanche. `sendHostNewCircleMember` réservé aux adhésions directes (`joinCircleDirectlyAction`). `memory/project_circle_join_via_event_single_notif.md`. |
| 2026-06-15 | **LinkedIn (OIDC)** ajouté comme 3e provider OAuth (avec Google/GitHub), `allowDangerousEmailAccountLinking: true` justifié par `email_verified` OIDC (respecte ADR-0001), intégré à la protection BotID. PR #542. **[→ ADR-0001](decisions/0001-oauth-account-linking.md)** |
| 2026-06-16 | **Gouvernance des décisions formalisée.** `spec/decisions.md` devient le registre canonique unique. Système à deux niveaux : ligne ici + ADR détaillé dans `spec/decisions/` pour les décisions lourdes. Table « Décisions prises » de `CLAUDE.md` remplacée par un pointeur (évite la divergence constatée : la table CLAUDE.md s'arrêtait en avril, ce journal allait jusqu'en mai). Règle normative ajoutée dans `CLAUDE.md` : consigner toute décision structurante sans attendre une demande explicite. |
| 2026-06-17 | **Mode maintenance via Vercel Edge Config + middleware.** Toggle de la clé `maintenance` propagé en secondes sans rebuild ; le middleware (`src/middleware.ts`) rewrite vers la page statique `/maintenance` en **503 + Retry-After**. **Fail-open** (lecture Edge Config qui échoue → trafic laissé passer, jamais couper le site à cause du mécanisme). **Bypass** maintainer via `?maintenance_bypass=<token>` (comparaison timing-safe, cookie httpOnly 8h). Page `/maintenance` = composant statique pur (zéro DB/auth/service externe), bilingue FR/EN via `Accept-Language`. Webhook Stripe et `/api/*` hors matcher → restent vivants. Override local `MAINTENANCE_MODE=true`. Scripts `pnpm maintenance:on|off|status` (API Vercel, sans dashboard). Alternatives écartées : env var + redeploy (trop lent, inutilisable si build cassé) ; toggle en DB (inutilisable si l'incident vient de la DB). Issue #545, `spec/features/maintenance-mode.md`. |
| 2026-06-16 | **Focus des champs de saisie = bordure neutre, jamais de ring rose.** Les primitives `input`/`textarea`/`select`/`place-autocomplete-input` et les champs custom (saisie de commentaire, formulaire de contact) passent de `focus-visible:ring-ring` (rose, couleur `--ring`) à `focus-visible:border-muted-foreground`, sans ring de focus (le ring rouge d'erreur `aria-invalid:ring` est conservé). Le ring de focus **reste** sur les contrôles interactifs non-texte (boutons, tabs, switch, badge, fermeture de dialog/sheet, calendrier) : indicateur d'accessibilité clavier. De même, la **sélection de texte** n'est plus surlignée en rose (`selection:bg-primary` retiré de `input`/`place-autocomplete-input`) : sélection neutre par défaut du navigateur, cohérente avec le `textarea`. Règle normative ajoutée au design system (`CLAUDE.md`). |
| 2026-06-18 | **Tout organisateur actif (HOST + CO_HOST) est automatiquement inscrit `REGISTERED` à chaque événement de son Circle.** Généralise l'auto-inscription jusqu'ici réservée au créateur : à la création de l'événement (tous les organisateurs via `findOrganizers`), à la promotion en co-host (tous les événements à venir, `startsAt > now` et `status ∈ {DRAFT, PUBLISHED}`), et par backfill direct en base pour l'existant. Insertion directe via `registrationRepository.create` → **aucune notification** (les emails ne partent que des server actions, pas du repository). **Les organisateurs ne peuvent pas se désinscrire** (règle D16 `OrganizerCannotCancelRegistrationError` préservée). **Zéro changement UI** : le co-host voit toujours « Gérer » et apparaît dans la liste des inscrits. Alternative écartée : découpler rôle (Circle) et présence (Moment) avec un parcours d'inscription/désinscription dédié aux organisateurs (distinction créateur/co-host, 3 états de CTA, bloc « Ta participation », modification de D16) — beaucoup plus de surface (domaine + UI + tests) pour un besoin marginal vu le faible volume de co-hosting. `spec/features/co-host-event-participation.md`. |
| 2026-06-19 | **Vercel BotID repassé en `observe` (ne bloque plus) — fiabilité jugée insuffisante.** Taux de faux positifs élevé sur le sign-in : 8 personnes flaggées sur 72 ayant atteint le sign-in en 5 jours (~11 %), **100 % sur navigateurs grand public** (Chrome 147/Mac, Firefox 140 Linux/Win), 0 user-agent de bot ; au moins 2 prospects réels bloqués (audience devs Linux/Firefox de l'événement Cloud Pi Native, dont 1 humain confirmé ayant tenté 4×). BotID sur-flague les profils privacy/Linux/Firefox/versions récentes, c'est-à-dire exactement notre audience. Conséquence : **plus de protection anti-bot active**, le vecteur « contacter les organisateurs » est de nouveau exposé. Recherche d'une solution alternative fiable lancée (issue #562, priorité high). Affine la décision BotID du 2026-06-14 ([ADR-0004](decisions/0004-botid-sign-in-fail-open.md)). |
| 2026-06-19 | **Upload des pièces jointes d'événement en client-direct (Vercel Blob).** Migration de la Server Action (plafond plateforme **4,5 Mo**, qui cassait déjà le « 10 Mo » en prod) vers `upload()` de `@vercel/blob/client` + route `handleUpload`, pour supporter les **vidéos** (MP4/MOV/WebM) jusqu'à **30 Mo**. Limite **différenciée** par type (`maxSizeForContentType` : vidéos 30 Mo, documents/images 10 Mo). **Renoncement au sniffing MIME serveur** (le serveur ne voit plus les octets ; on se fie à `allowedContentTypes`, risque borné : HOST authentifié + rate-limit + pas de SVG). Persistance via `confirmMomentAttachmentAction` (pas `onUploadCompleted`, inopérant en localhost) avec suppression du blob en cas de rejet (anti-orphelin). Vraie barre de progression (`onUploadProgress`). Alternatives écartées : Server Action (impossible >4,5 Mo), hybride vidéo/document (deux systèmes + documents plafonnés à 4 Mo). Dette connexe : cover image et avatar ont le même bug latent 4,5 Mo (hors périmètre). `spec/features/moment-video-attachments.md`. **[→ ADR-0005](decisions/0005-client-direct-attachment-upload.md)** |
